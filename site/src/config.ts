// One place for anything that would otherwise be pasted into several components.
export const REPO = 'https://github.com/krlosdev/BatonPass';

// The releases page rather than a direct asset URL: asset filenames carry the
// version, so a direct link would break on every release. GitHub picks the
// latest for us and the page never has to know the current version number.
export const RELEASES = `${REPO}/releases/latest`;

export const AUTHOR = 'krlosdev';
